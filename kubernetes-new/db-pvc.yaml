apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  # volumeMode: Block
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
